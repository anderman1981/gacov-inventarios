# Frontend GACOV

Este directorio concentra los assets fuente del frontend:

- `resources/css/`
- `resources/js/`
- `resources/images/`

El build se sigue ejecutando desde la raíz del proyecto con `npm run build` y publica los archivos compilados en `public/build` para que Laravel los sirva con `@vite(...)`.
